c     ----------------------------------
c     definitions of numerical constants
c     ----------------------------------

      include 'a_constant.h'

      real*8 onehalf
      parameter ( dsmall  = 1.e-7    )     ! small displacement
      parameter ( small   = 1.e-7    )
      parameter ( nil     = 0        )     ! integer zero 
      parameter ( zero    = 0.0      )     ! real zero
      parameter ( onehalf = 0.5      )     ! real half
      parameter ( one     = 1.0      )     ! real one
      parameter ( two     = 2.0      )     ! real two 

